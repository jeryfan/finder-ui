[project]
name = "server"
version = "0.1.0"
description = "FastAPI backend for finder-ui"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.135.1",
    "mangum>=0.21.0",
    "python-multipart>=0.0.22",
    "uvicorn>=0.41.0",
]
